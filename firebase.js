// Добавьте конфигурацию PayPal (замените на реальную)
const paypalConfig = {
  clientId: "ВАШ_CLIENT_ID",
  currency: "USD"
};

// Экспортируйте для использования в других файлах
window.firebaseConfig = firebaseConfig;
window.paypalConfig = paypalConfig;
